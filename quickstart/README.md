# Hello

## Make a kotlin project

``` shell
mkdir hello
cd hello
gradle init --type kotlin-application --dsl kotlin
```

## run, build, test

``` shell
./gradlew run
```

``` shell
./gradlew build
```

``` shell
./gradlew test
```
